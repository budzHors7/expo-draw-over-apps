import React from 'react';
import VersionedContentPage from '@site/src/components/VersionedContentPage';

export default function LatestFixtures(): JSX.Element {
  return <VersionedContentPage pageKey="fixtures" routeSegment="latest" versionKey="56" />;
}

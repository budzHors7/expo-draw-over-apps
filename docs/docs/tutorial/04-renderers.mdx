---
title: Renderers
sidebar_position: 4
description: Register custom React Native and Compose-flavored bubble renderers for expo-draw-over-apps.
---

# Renderers

The default bubble is a React Native renderer. You can replace it globally or per bubble.

## React Native renderer

```tsx
import { useEffect } from 'react';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import {
  type BubbleRendererProps,
  setOverlaySharedValue,
  setBubbleRendererForBubble,
  useOverlaySharedValueState,
} from 'expo-draw-over-apps';

const bubbleId = 'chat-head';
const counterKey = 'chat-head-counter';

function ChatHeadBubble({ close, openApp }: BubbleRendererProps) {
  const counterState = useOverlaySharedValueState(counterKey);

  return (
    <View style={styles.bubble}>
      <Text style={styles.label}>Counter</Text>
      <Text style={styles.count}>{counterState.value}</Text>

      <View style={styles.row}>
        <Pressable
          onPress={() => setOverlaySharedValue(counterKey, Math.max(0, counterState.value - 1), 'bubble')}
          style={styles.button}
        >
          <Text style={styles.buttonText}>-</Text>
        </Pressable>
        <Pressable
          onPress={() => setOverlaySharedValue(counterKey, counterState.value + 1, 'bubble')}
          style={styles.button}
        >
          <Text style={styles.buttonText}>+</Text>
        </Pressable>
      </View>

      <Pressable onPress={() => void openApp()} style={styles.secondaryButton}>
        <Text style={styles.secondaryText}>Open app</Text>
      </Pressable>
      <Pressable onPress={close} style={styles.secondaryButton}>
        <Text style={styles.secondaryText}>Close</Text>
      </Pressable>
    </View>
  );
}

export function RegisterChatHeadBubble() {
  useEffect(() => {
    setBubbleRendererForBubble(bubbleId, ChatHeadBubble);
    return () => setBubbleRendererForBubble(bubbleId, null);
  }, []);

  return null;
}

const styles = StyleSheet.create({
  bubble: {
    width: 200,
    borderRadius: 24,
    padding: 16,
    gap: 12,
    backgroundColor: '#111827',
  },
  label: {
    color: '#86efac',
    textAlign: 'center',
  },
  count: {
    color: '#ffffff',
    fontSize: 32,
    fontWeight: '800',
    textAlign: 'center',
  },
  row: {
    flexDirection: 'row',
    gap: 10,
  },
  button: {
    flex: 1,
    borderRadius: 16,
    paddingVertical: 12,
    alignItems: 'center',
    backgroundColor: '#2563eb',
  },
  buttonText: {
    color: '#ffffff',
    fontSize: 20,
    fontWeight: '800',
  },
  secondaryButton: {
    borderRadius: 14,
    paddingVertical: 10,
    alignItems: 'center',
    backgroundColor: '#e5e7eb',
  },
  secondaryText: {
    color: '#111827',
    fontWeight: '700',
  },
});
```

## Reanimated window containers

Use the built-in window containers when a renderer changes size. They keep resize animation inside Reanimated instead of repeatedly driving overlay layout through React state.

```tsx
import { Pressable, StyleSheet, Text } from 'react-native';
import {
  type BubbleRendererProps,
  NativeWindowContainer,
  ReactNativeWindowContainer,
  setOverlaySharedValue,
  useOverlaySharedValueState,
} from 'expo-draw-over-apps';

const resizeKey = 'window-container-counter';

export function SmoothReactNativeBubble(_: BubbleRendererProps) {
  const sharedStep = useOverlaySharedValueState(resizeKey);
  const expanded = sharedStep.value >= 1;

  function toggleSize() {
    setOverlaySharedValue(resizeKey, expanded ? 0 : 1, 'bubble');
  }

  return (
    <ReactNativeWindowContainer
      width={expanded ? 238 : 154}
      height={expanded ? 256 : 172}
      borderRadius={expanded ? 44 : 28}
      style={styles.reactNativeWindow}
      contentContainerStyle={styles.centeredContent}
    >
      <Pressable onPress={toggleSize}>
        <Text style={{ color: 'white', fontWeight: '900' }}>Toggle size</Text>
      </Pressable>
    </ReactNativeWindowContainer>
  );
}

export function SmoothExpoUiBubble(_: BubbleRendererProps) {
  const sharedStep = useOverlaySharedValueState(resizeKey);
  const expanded = sharedStep.value >= 1;

  function toggleSize() {
    setOverlaySharedValue(resizeKey, expanded ? 0 : 1, 'bubble');
  }

  return (
    <NativeWindowContainer
      width={expanded ? 238 : 154}
      height={expanded ? 256 : 172}
      borderRadius={expanded ? 44 : 28}
      style={styles.nativeWindow}
      contentContainerStyle={styles.centeredContent}
    >
      <Pressable onPress={toggleSize}>
        <Text style={{ color: '#0f172a', fontWeight: '900' }}>Toggle size</Text>
      </Pressable>
    </NativeWindowContainer>
  );
}

const styles = StyleSheet.create({
  reactNativeWindow: {
    borderWidth: 2,
    borderColor: '#fb7185',
    padding: 16,
    backgroundColor: '#020617',
  },
  nativeWindow: {
    borderWidth: 2,
    borderColor: '#0891b2',
    padding: 16,
  },
  centeredContent: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
```

`NativeWindowContainer` uses Expo UI's Android Host/Surface when `@expo/ui` is installed and falls back to `ReactNativeWindowContainer` otherwise. Both paths accept React Native children. When no background or `colorScheme` is declared, the containers follow the device light/dark setting.

## Compose-flavored renderer

`setComposeBubbleRenderer()` registers the packaged `@expo/ui` entry point.

```tsx
import { useEffect } from 'react';
import {
  setBubbleRendererForBubble,
  setComposeBubbleRenderer,
} from 'expo-draw-over-apps';

const bubbleId = 'jetpack-compose-counter';

export function RegisterComposeBubble() {
  useEffect(() => {
    setComposeBubbleRenderer(bubbleId);
    return () => setBubbleRendererForBubble(bubbleId, null);
  }, []);

  return null;
}
```

Current Android overlay builds use a React Native fallback for this packaged renderer. The API is present, but the live overlay surface does not mount a Compose view yet.
